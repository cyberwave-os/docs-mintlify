---
title: "RF Scanner Pod"
description: "Redirect stub to the RF Scanner Pod catalog page."
canonical: "https://cyberwave.com/generic-drone-payloads/catalog/rf-scanner-pod"
---

import { useEffect } from "react";

export default function RFScannerPodRedirectPage() {
  useEffect(() => {
    window.location.replace("https://cyberwave.com/generic-drone-payloads/catalog/rf-scanner-pod");
  }, []);

  return (
    <>
      # Redirecting to RF Scanner Pod

      If you are not redirected automatically, [open the RF Scanner Pod catalog page](https://cyberwave.com/generic-drone-payloads/catalog/rf-scanner-pod).
    </>
  );
}
